{
  "endpoint": "/api/workflows/:id/start",
  "method": "POST",
  "taskId": "145a31aa-6e9c-4834-b373-083839d339ac",
  "requiresAuth": true,
  "priority": 33,
  "dependsOn": ["POST /api/workflows"],
  "testCases": [
    {
      "name": "Start the signing process for a workflow",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "urlParams": {
        "id": "{{cache:workflows.create.response.data.workflow.id}}"
      },
      "expectedStatus": 200,
      "expectedResponse": {
        "success": true,
        "data": {
          "workflow": {
            "id": "string",
            "status": "string",
            "recipients": "array"
          }
        }
      }
    }
  ]
}
